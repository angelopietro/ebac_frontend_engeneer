function multiplicacao(valorA: number, valorB: number): number {
    return valorA * valorB;    
}

const respostaMultiplicacao = multiplicacao(10, 12);

console.log(respostaMultiplicacao);



function saudacao(nome: string): string {
    return `Olá ${nome}`;   
}

const respostaSaudacao = saudacao('Ângelo Pietro');

console.log(respostaSaudacao);

